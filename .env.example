# Copy this file to .env and adjust as needed.
# Port for the Vite dev server (npm run dev). Defaults to 5173 if unset.
PORT=5173
# Number of individually turnable page leaves in the book. Defaults to 16 if unset.
VITE_MAX_PAGES=16
