// The wrought-iron wall sconces, three candles to each, burning the
// candle's own flame. Where they hang is the layout's call; the offset says
// how far the origin stands from the wall.
export { default as Sconce } from './Sconce'
export { SCONCE_BACK_OFFSET } from './domain/sconce'
